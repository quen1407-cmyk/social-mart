#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../cross-env/src/bin/cross-env.js" "$@"
else
  exec node  "$basedir/../../../../cross-env/src/bin/cross-env.js" "$@"
fi
# cmd-shim-target=C:/Users/MyBook Hype AMD/social-mart/node_modules/.pnpm/@react-three+drei@10.0.8_@r_50988723d9226e673ef7e1ff194a9edb/node_modules/cross-env/src/bin/cross-env.js
